import React from "react";
import 'index.css';
import { NavBar } from "../../Componets";

type Props = {}
export const Main = (props:Props) =>{
    return(
        <div className="Main">
            <NavBar/>
        </div>
    );
};